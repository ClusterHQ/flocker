scenarios:
  default:
    type: no-load

operations:
  default:
    type: read-request

  no-op:
    type: no-op

  wait-10:
    type: wait
    wait_seconds: 10

metrics:
  default:
    type: wallclock

  cputime:
    type: cputime

  wallclock:
    type: wallclock
